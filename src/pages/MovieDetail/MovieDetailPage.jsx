export default function MovieDetailPage(){
    return(
        <h1>MovieDetailPage</h1>
        // Renders when a movie is clicked on MoviesList
        // useParams hook to access movieName
        // 
    )
}